/**
 * @deprecated Import from workspace-view-model directly.
 * This shim exists for backwards compatibility during the renderer store refactor.
 */
export { WorkspaceViewModel as TaskViewStore, type RendererKind } from './workspace-view-model';
